{
  "name": "subtrack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd subtrack-backend && npm install && cd ../subtrack-frontend && npm install",
    "build:all": "cd subtrack-backend && npm run build && cd ../subtrack-frontend && npm run build"
  }
}
